{{- if not .Values.models }}
apiVersion: serving.kserve.io/v1alpha1
kind: LLMInferenceService
metadata:
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
    openshift.io/display-name: {{ include "llminferenceservice.displayname" . }}
    {{- with .Values.modelType }}
    opendatahub.io/model-type: {{ quote . }}
    {{- end }}
    {{- with .Values.genaiAsset }}
    {{- if .enabled }}
    opendatahub.io/genai-use-case: {{ quote .useCase }}
    openshift.io/description: {{ quote .description }}
    {{- end }}
    {{- end }}
    {{- if and .Values.enableAuth (not .Values.maas.enabled) }}
    security.opendatahub.io/enable-auth: "true"
    {{- else if not .Values.maas.enabled }}
    security.opendatahub.io/enable-auth: "false"
    {{- end }}
    {{- with .Values.hardwareProfile }}
    {{- if .enabled }}
    opendatahub.io/hardware-profile-name: {{ .name }}
    opendatahub.io/hardware-profile-namespace: {{ .namespace }}
    {{- end }}
    {{- end }}
  labels:
    {{- include "llminferenceservice.labels" $ | nindent 4 }}
    opendatahub.io/dashboard: "true"
    {{- if or .Values.genaiAsset.enabled .Values.maas.enabled }}
    opendatahub.io/genai-asset: "true"
    {{- end }}
  name: {{ include "llminferenceservice.fullname" . }}
spec:
  model:
    name: {{ include "llminferenceservice.fullname" . }}
    {{- if .Values.model.uri }}
    {{- if .Values.model.pvc.preload }}
    uri: pvc://{{ include "llminferenceservice.fullname" . }}-model
    {{- else }}
    uri: {{ .Values.model.uri }}
    {{- end }}
    {{- end }}
  {{- if and .Values.replicas (not .Values.autoscaling.enabled) }}
  replicas: {{ .Values.replicas }}
  {{- end }}
  {{- with .Values.autoscaling }}
  {{- if .enabled }}
  scaling:
    minReplicas: {{ .minReplicas }}
    maxReplicas: {{ .maxReplicas }}
    wva:
      {{- toYaml .wva | nindent 6 }}
  {{- end }}
  {{- end }}
  router:
    gateway:
      refs:
        {{- if .Values.maas.enabled }}
        - name: maas-default-gateway
        {{- else }}
        - name: openshift-ai-inference
        {{- end }}
          namespace: openshift-ingress
    route: {}
    scheduler: {}
  template:
    containers:
      - name: main
        image: {{ include "llminferenceservice.image" . }}
        {{- if or .Values.model.args .Values.env }}
        env:
          {{- with .Values.model.args }}
          - name: VLLM_ADDITIONAL_ARGS
            value: >-
              {{- join " " . | wrap 106 | nindent 14 }}
          {{- end }}
          {{- range $name, $value := .Values.env }}
          - name: {{ $name }}
            value: {{ $value | quote }}
          {{- end }}
        {{- end }}
        {{- if or .Values.huggingface.token .Values.huggingface.existingSecret }}
        envFrom:
            - secretRef:
                name: {{ .Values.huggingface.existingSecret | default (printf "%s-token" (include "llminferenceservice.fullname" .)) }}
        {{- end }}
        resources:
          {{- include "llminferenceservice.resources" . | nindent 10 }}
    {{- $ns := include "llminferenceservice.nodeSelector" . | trim }}
    {{- if $ns }}
    nodeSelector:
      {{- $ns | nindent 6 }}
    {{- end }}
    {{- with .Values.affinity }}
    affinity:
      {{- toYaml . | nindent 6 }}
    {{- end }}
    {{- with .Values.tolerations }}
    tolerations:
      {{- toYaml . | nindent 6 }}
    {{- end }}
{{- end }}
