global:
  toolsImage: image-registry.openshift-image-registry.svc:5000/openshift/tools:latest

install-observability:
  enabled: true
  operators:
    tempo-product:
      channel: stable
      installPlanApproval: Manual
      namespace: openshift-tempo-operator
      operatorGroup:
        enabled: true
      startingCSV: tempo-operator.v0.20.0-3
    cluster-observability-operator:
      channel: stable
      installPlanApproval: Manual
      namespace: openshift-cluster-observability-operator
      operatorGroup:
        enabled: true
      startingCSV: cluster-observability-operator.v1.4.0
    opentelemetry-product:
      channel: stable
      installPlanApproval: Manual
      namespace: openshift-opentelemetry-operator
      operatorGroup:
        enabled: true
      startingCSV: opentelemetry-operator.v0.144.0-3

# Optional workaround for COO-784 (OOM on perses-operator after COO 1.0). Fixed in COO 1.1.1+.
# The patch Job is a Helm hook: if it waits for perses-operator before COO is Ready,
# helm upgrade stays InProgress until timeout. Keep enabled only on COO 1.0/1.1.0.
csvPatch:
  csvName: cluster-observability-operator.v1.4.0
  subscription: cluster-observability-operator

persesOperator:
  resourcesPatch:
    enabled: false
    requests:
      memory: 3Gi
      cpu: 200m
    limits:
      memory: 5Gi
      cpu: 500m
