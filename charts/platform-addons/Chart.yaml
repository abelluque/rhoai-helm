---
apiVersion: v2
name: platform-addons
description: >
  Cluster add-ons: Nutanix Files StorageClass/PVCs, Model Registry
  (MySQL + MinIO + ModelRegistry CR), OpenShift GitOps and Pipelines
  operators, and platform node placement (non-GPU VMs).
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: install-operators
    version: 0.1.0
    repository: file://../install-operators
    alias: install-platform-operators
    condition: install-platform-operators.enabled
